import type { Compiler } from "webpack";
import type { Options } from "./Options";
declare class AsyncCssPlugin {
    constructor(options?: Options);
    apply({ hooks }: Compiler): void;
    private static assertUnreachable;
    private readonly options;
    private logged;
    private log;
    private checkHook;
    private doLog;
    private checkTags;
    private processTag;
}
export = AsyncCssPlugin;
